---
title: "DFS入门"
icon: "edit"
date: 2023-05-08
category:
  - "算法"
  - "DFS"
tag:
  - "算法"
  - "DFS"
description: "引出搜索中的DFS DFS本质上也是一直递归调用，比如我们要构造出所有0000到1111的二进制数，我们可以发现它是有规律可寻得: img 由递归引出DFS 因为DFS基本上都是通过递归实现，但递归和DFS又不一样"
---
#### 引出搜索中的DFS

DFS本质上也是一直递归调用，比如我们要构造出所有0000到1111的二进制数，我们可以发现它是有规律可寻得:

![](https://skyme-1307417630.cos.ap-chengdu.myqcloud.com/blog/asynccode)

#### 由递归引出DFS

因为DFS基本上都是通过递归实现，但递归和DFS又不一样

那么什么是DFS呢？

在**搜索算法**中，该词常常指利用递归函数方便地实现暴力枚举的算法，与图论中的 DFS 算法有一定相似之处，但并不完全相同。

DFS 最显著的特征在于其**递归****调用自身**。同时与 BFS 类似，DFS 会对其访问过的点打上访问标记，在遍历图时跳过已打过标记的点，以确保**每个点仅访问一次**。符合以上两条规则的函数，便是广义上的 DFS。

#### 题目：跳台阶

![](https://skyme-1307417630.cos.ap-chengdu.myqcloud.com/blog/asynccode)

##### 思路：

先找出台阶方案数的规律，可以发现是一个斐波那契数列

![](https://skyme-1307417630.cos.ap-chengdu.myqcloud.com/blog/asynccode)

由此我们可以构造一个递归函数进行，递归通常是由顶向下的，也就是到最后才会满足结束条件

在这道题我们每次去调用当前数的前两个数就可以了(比如当前为x，那么调用x-1+x-2)

##### 代码：

```Java
package DFS;

import java.util.Scanner;

public class 跳台阶 {

        //通过规律找出跳台阶的方案数是斐波那契数列

        public static int fib(int n) {

                if(n==1)return 1;

                if(n==2) return 2;

                return fib(n-1)+fib(n-2);

        }

        public static void main(String[] args) {

                Scanner scanner=new Scanner(System.in);

                int n=scanner.nextInt();

                System.out.print(fib(n));

        }

}

```

### 三种递归类型模版：

![](https://skyme-1307417630.cos.ap-chengdu.myqcloud.com/blog/asynccode)

#### 递归实现指数级枚举：

一般是前后不影响，每次选择的方式是固定的

![](https://skyme-1307417630.cos.ap-chengdu.myqcloud.com/blog/asynccode)

##### 思路：

题目要求我们枚举出所有的情况，实际就是进行枚举，那么我们可以通过递归(DFS）搜索树将所有情况输出

![](https://skyme-1307417630.cos.ap-chengdu.myqcloud.com/blog/asynccode)

##### 代码：

```Java
package DFS;
import java.util.Scanner;
import Sousuo.dfs;
public class 递归实现指数型枚举 {
        static int st[]=new int [16];
        static int n;

        //选择的方案有2的n次方种

        public static void dfs(int x) {

                if(x>n) {

                        for(int i=1;i<=n;i++) {

                                if(st[i]==1)

                                System.out.print(i+" ");

                        }

                        System.out.println();

                        return;

                }

                //选

                st[x]=1;

                dfs(x+1);

                st[x]=0;//恢复状态

                //不选

                st[x]=2;

                dfs(x+1);

                st[x]=0;

        }

public static void main(String[] args) {

        Scanner scanner=new Scanner(System.in);

        n=scanner.nextInt();

        dfs(1);

}

}

```

#### 递归实现排列型枚举:

![](https://skyme-1307417630.cos.ap-chengdu.myqcloud.com/blog/asynccode)

##### 思路：

按位置来选,分为1到n个位置，并且每个位置都会受前一个的影响,题目中要求我们由字典序排序，所以说dfs的时候也要从1进行枚举赋值,当然dfs，这里可以理解为递归当然有退出条件就是当我们枚举的位置大于n的时候退出

##### 代码：

```Java
package DFS;

import java.util.Scanner;

public class 递归实现排列型枚举 {

        static int n;

        static int arr[];//位置数字，每个位置具体的数字

        static  int st[]=new int[11];//因为前一个会影响下一个的选取所以这个数组是是否枚举过的flag

        public static void dfs(int x) {

                if(x>n) {

                for(int i=1;i<=n;i++) {

                        System.out.print(arr[i]+" ");

                }

                System.out.println();

                return;

                }

                for(int i=1;i<=n;i++) {

                        if(st[i]==0) {

                                st[i]=1;

                                arr[x]=i;

                                dfs(x+1);

                                st[i]=0;

                                arr[x]=0;

                        }

                }

        }

        public static void main(String[] args) {

                Scanner scanner=new Scanner(System.in);

                n=scanner.nextInt();

                arr=new int[n+1];

                dfs(1);

        }

}

```

#### 递归实现组合型枚举：

![](https://skyme-1307417630.cos.ap-chengdu.myqcloud.com/blog/asynccode)

##### 思路：

组合和排列很像，但是二者的区别在于排列是所有的情况，而组合还有限制条件，比如前一个数要比后一个数小

![](https://skyme-1307417630.cos.ap-chengdu.myqcloud.com/blog/asynccode)

怎么去解决呢，如果按照排列的思路的话，我们可能去选择比我们小的，所以我们增加一个参数，那么就可以满足每次都是从下一个比前一个大的开始了。

##### 代码：

```Java
package DFS;

import java.util.Scanner;

public class 递归实现组合型枚举2 {

        static int n,m;

        static int st[]=new int[30];

        static int arr[];

        public static void dfs(int x,int s) {

                //加入了start参数，这样每次DFS的时候都是从下一个数开始，满足顺序

                if(x>m) {

                        for(int i=1;i<=m;i++) {

                                System.out.print(arr[i]+" ");

                        }

                        System.out.println();

                        return;

                }

                for(int i=s;i<=n;i++) {

                        st[i]=1;

                        arr[x]=i;

                        dfs(x+1, i+1);

                        //回溯

                        arr[x]=0;

                        st[i]=0;

                }

        }

        public static void main(String[] args) {

                Scanner scanner=new Scanner(System.in);

                n=scanner.nextInt();

                m=scanner.nextInt();

                arr=new int[m+1];

                dfs(1,1);

        }

}

```

### 巩固：

#### 选数：

![](https://skyme-1307417630.cos.ap-chengdu.myqcloud.com/blog/asynccode)

##### 思路：

首先我们看案例，就是从4个给的数里面去选3个数，然后把所有组合（为什么是组合，因为我们不能找重复的！）的和找出是否为质数，这里为什么不会有重复，因为是组合枚举，意味着不会重复，如何判断是否是质数，可以用筛子，也可以用一个函数判断是否是质数

```C++
//判断单个是否为质数
bool isprime(int a){
    for(int i = 2; i * i <= a; i++)
        if(a % i == 0)
            return false;
    return true;
}
```

##### 代码：

```Java
package DFS;

import java.util.Scanner;

public class 选数 {

        static boolean st[]=new boolean[10000001];

        static int a[];

        static int k,n;

        static int vis[];

        static int arr[];

        static int cnt=0;

        //先用筛子将素数筛出来

        //然后进行选择

        static public void get(int n) {

                st[1]=true;

                for(int i=2;i<=n;i++) {

                        if(!st[i]) {

                                for(int j=i+i;j<=n;j+=i) {

                                        st[j]=true;

                                }

                        }

                }

        }

        static public void dfs(int x,int s) {

                if(x>k) {

                        int sum=0;

                        for(int i=1;i<=k;i++) {

                                sum+=arr[i];

                        }

                        if(!st[sum])cnt++;

                        return;

                }

                for(int i=s;i<=n;i++) {

                        if(vis[i]==0) {

                                vis[i]=1;

                                arr[x]=a[i];

                                dfs(x+1,i+1);

                                vis[i]=0;

                                arr[x]=0;

                        }

                }

        }

        public static void main(String[] args) {

                get(99999);

                Scanner scanner=new Scanner(System.in);

                 n=scanner.nextInt();

                a=new int[n+1];

                k=scanner.nextInt();

                vis=new int[n+1];

                arr=new int[k+1];

                for(int i=1;i<=n;i++) {

                        a[i]=scanner.nextInt();

                }

                dfs(1,1);

                System.out.println(cnt);

        }

}

```

#### 烤鸡：

![](https://skyme-1307417630.cos.ap-chengdu.myqcloud.com/blog/asynccode)

##### 思路：

这道题很显然的是DFS，因为题目告诉了你有10种配料，而且每种配料可以放1到3克，要求你去找所有搭配方案，很容易想到我们使用dfs进行问题求解,因为我们之前学了三种模版，无非就是这三种的各种变种罢了

![](https://skyme-1307417630.cos.ap-chengdu.myqcloud.com/blog/asynccode)

很显然有十个位置，每个位置我们去选择配料为多少克

每个位置我们可以去选择1,2,3，到什么时候退出呢，要么总共的和大于了n，或者位置达到了10并且要么等于n,这个时候输出即可

每次的选择都不会受前一个位置的影响，说明是指数枚举

##### 代码：

```Java
package DFS;

import java.util.Scanner;

import Sousuo.dfs;

public class 烤鸡 {
        static int st[]=new int[11];
        static int arr[]=new int[11];
        static int n;
        static int cnt=0;
        static int ans[][]=new int[59055][11];
        public static void dfs(int x,int sum) {
                if(sum>n)return;
                if(x>10) {
                        if(sum==n) {
        for(int i=1;i<=10;i++) {
                        ans[cnt][i]=arr[i];
                        }
                        cnt++;
                        }
                return;
                }

                                //选择1
                                st[x]=1;
                                arr[x]=1;
                                dfs(x+1,sum+1);
                                st[x]=0;
                                arr[x]=0;
                                //选择2
                                st[x]=1;
                                arr[x]=2;
                                dfs(x+1,sum+2);
                                st[x]=0;
                                arr[x]=0;

                                //选择3
                                st[x]=1;
                                arr[x]=3;
                                dfs(x+1,sum+3);
                                st[x]=0;
                                arr[x]=0;

        }
        public static void main(String[] args) {
                Scanner scanner=new Scanner(System.in);
                n=scanner.nextInt();
                dfs(1,0);
                System.out.println(cnt);
                for(int i=0;i<cnt;i++) {
                        for(int j=1;j<=10;j++) {
                                System.out.print(ans[i][j]+" ");
                        }
                        System.out.println();
                }
        }

}
```

#### 火星人：

[https://www.luogu.com.cn/problem/P1088](https://www.luogu.com.cn/problem/P1088)

##### 思路：

很显然这道题是全排列
